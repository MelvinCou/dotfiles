#!{{ lookPath "sh" }}
set -eu

echo "Installing packages"
sudo apt-get update -qqy
# Those packages may not be available depending of distro variant
sudo apt-get install -qqy --no-install-recommends {{ .packages.debian.apt | join " " }} 1>/dev/null || true
